---
classes:
  - buildbot_slave
  - loggy
  - rvm

buildbot_slave::slave_name: 'bb_slave3_ubuntu'

# mkdocs needed for eagle docs build

python::python_pips:
  mkdocs:
    ensure: present

rvm::system_users:
  - buildslave29

rvm::rvm_gems:
  'jekyll':
    name: 'jekyll'
    ensure: latest
